[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable && corepack prepare pnpm@9 --activate && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @askbase/api build"]

[start]
cmd = "pnpm --filter @askbase/api start"
