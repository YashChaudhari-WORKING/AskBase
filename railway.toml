[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @askbase/api build"

[deploy]
startCommand = "pnpm --filter @askbase/api start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
